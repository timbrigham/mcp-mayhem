__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
*.tmp
# Real data + integrity sidecars. Everything the server writes under data/ is
# treated as private live data and kept out of git. The one exception is the
# demo/fixture sample.json, which is safe to track.
data/*
!data/sample.json
