{
  "schema": "zp.admission.v1",
  "_what": "THE ADMISSION SET: which registered verdict types must be green for gitRobot to let an action through. This is NOT the registry. The registry (verdictLedger's required.v2.json) says what may be RECORDED; this says what must PASS.",
  "_why_here": "Separation of duty. The ledger records verdicts; if it also decided what was SUFFICIENT, anyone able to edit ledger config could lower the bar that admits a push in one place. Split them and two systems must agree before the bar moves. This is a deliberate, NARROW reversal of 'no policy values in gitRobot' -- the type registry, min_passes, max_depth, lock waits, the genesis floor and HOW `complete` is computed all stay in the ledger, and gitRobot never re-derives completeness. It names this set and requires the ledger's answer.",
  "_complete_not_partial": "Tim, 2026-08-23: 'I want the complete set, not a partial lets do this later.' A PARTIAL ADMISSION SET IS A HALF-WORKING GATE. Admit 3 of 19 and the other 16 do not gate -- which is indistinguishable, from the outside, from the state that passed the push it should not have. That is the same reasoning that got the branch reset.",
  "_rely_not_admitted_at_commit_or_push": "Tim, 2026-08-26: 'I didn't ever want this gone. I just wanted it to be able to work.' `rely` stays REGISTERED and RECORDABLE and keeps gating a TAG. It no longer admits commit or push, for two measured reasons. (1) CIRCULARITY: its scope is tools/verify/*, so it reviews the verification tooling -- and every fix to that tooling stales it while it gates the commit that would carry the fix. The whole R-1/R-2/R-4 remediation edits tools/verify/, so the loop deadlocks. (2) UNSATISFIABLE BY CONSTRUCTION: the registry declares scope tools/verify/* (60 files) while rely.md's own pre-flight says 'SCOPE IT -- a directory, a file list, or a named interface. Do not run it at full.' A gate required to cover a scope its own brief forbids covering can never be SATISFIED. That is not a missing declaration, it is a contradiction between the registry and the brief. WHY IT IS NOT DELETED: rely is the only agent that EXECUTES rather than reads, and its own measured law is that every BEDROCK finding came from executing while every ORDINARY one came from reading. Briefed to attack the controls it found 6 BLOCKING, all six in the control written to fix the previous pass -- including getting guards.py to print 13/13 ok over a completely neutered push gate. Removing the only thing that executes, because it is inconvenient during a sweep, is the 'treat a block as an obstacle' failure this server exists to resist. It keeps running and recording; it stops blocking. TAG is where 'can the tooling be relied on' is the question that matters, because a release mints a permanent DOI. It remains visible: the ledger reports it under `registered_not_admitting`, so a type that records without gating is stated, never silent.",
  "_check_claude_md_promoted": "PROMOTED 2026-08-26. `check_claude_md` gates commit, push and tag. It is NOT circular the way `rely` was -- it reviews CLAUDE.md, not the verification tooling -- so editing CLAUDE.md, re-running it and committing is an ordinary loop with no deadlock. WHAT A GREEN ROW ACTUALLY CLAIMS, because it is narrower than the name suggests and the checker says so itself at line 257: 'THE VERDICT IS THE BLOCKING LEGS ONLY.' There are exactly TWO blocking legs -- rooted paths resolve, and named checkers exist. Both are dead-pointer defects, the class CLAUDE.md accumulates and nobody checks, so they are worth having. WHAT IT DOES NOT CLAIM: `cap` is a WARN leg, so an entry over the 12-line cap still records a PASS. The ZP session cited R-NOTINLIB reaching 36 lines against that cap as the cost of NOT promoting this -- measured at the source, promoting does not fix that case, because a WARN leg does not fail the checker. Three further legs (trigger, budget, cites) are declared PENDING and are, in the checker's own words, 'NOT checked and NOT passing'. Promote it for the two properties it has, not for the shape contract as a whole. WHY IT IS CHEAP, and the mechanism is not the one it looks like: `when: \"CLAUDE.md\"` does NOT skip commits that leave the file alone. `when` is evaluated against the TRACKED FILE SET, and CLAUDE.md is always tracked, so the glob always matches and the type is always required. Measured 2026-08-26: the row reads SATISFIED, not NOT_APPLICABLE. The cheapness comes from CONTENT STALENESS -- the existing record keeps covering the unchanged blob, and only an edit to CLAUDE.md moves it to STALE. That is the stronger of the two behaviours: 'checked and still current' says more than 'does not apply'.",
  "_every_push_blocks_and_that_is_the_gate_working": "With the complete set admitted and no emitters wired, EVERY PUSH BLOCKS. That is deliberate. DO NOT THIN THIS SET TO UNBLOCK A PUSH -- satisfy the keys instead. Until checkers emit, the sanctioned way to satisfy one is the SIGNATURE ROUTE (decided.how = 'signature', who = '<name>'), which needs no agreement threshold and no paid agent round. A human accepting a key is attributable; a key that never had to exist is not.",
  "_empty_REFUSES": "An EMPTY list for an action REFUSES that action on the main repo; it does not allow-with-a-warning. That was the second half of the 2026-08-23 failure: EMPTY rendered as ALLOWED plus a capitalised warning, which is fail-OPEN in the costume of fail-closed. An empty set means the action would be certified against zero requirements.",
  "_mirrors_the_registry_and_the_registry_wins": "These lists mirror the registry's own `actions` narrowings. If admission and the registry ever disagree on an action, THE REGISTRY IS THE BUG -- it is the list that carries stated reasons. Admission is a list of names, and a list of names cannot explain itself, which is why a policy exception must never be expressed by thinning it.",
  "_genesis_is_admitted_by_nothing": "It is the recording floor, not a gate; the registry already narrows it to actions: [] with a reason.",
  "_conditional_types_are_still_admitted": "pdf_coupling is admitted everywhere and gates nothing when no PDF changed -- its registry `when` glob renders it NOT_APPLICABLE, carrying the glob. Admitting a conditional type is correct: 'it did not apply' and 'it was not required' are different facts, and only the first is checkable.",
  "default": "NOT_ADMITTING",
  "admission": {
    "commit": [
      "build",
      "check_checkers",
      "check_classes",
      "check_claude_md",
      "check_encoding",
      "check_figures",
      "check_hashes",
      "check_invariants",
      "check_modal",
      "check_moved",
      "check_negatives",
      "check_paths",
      "check_pov",
      "check_prose",
      "claim_review",
      "decls",
      "guards",
      "pdf_coupling"
    ],
    "push": [
      "adversary",
      "build",
      "check_checkers",
      "check_classes",
      "check_claude_md",
      "check_encoding",
      "check_figures",
      "check_hashes",
      "check_invariants",
      "check_modal",
      "check_moved",
      "check_negatives",
      "check_paths",
      "check_pov",
      "check_prose",
      "claim_review",
      "decls",
      "editorial",
      "guards",
      "pdf_coupling",
      "prior_art"
    ],
    "tag": [
      "adversary",
      "build",
      "check_checkers",
      "check_classes",
      "check_claude_md",
      "check_encoding",
      "check_figures",
      "check_hashes",
      "check_invariants",
      "check_modal",
      "check_moved",
      "check_negatives",
      "check_paths",
      "check_pov",
      "check_prose",
      "claim_review",
      "decls",
      "editorial",
      "guards",
      "pdf_coupling",
      "release_ready",
      "rely"
    ]
  },
  "_req2_2026_08_23": "rely and claim_review admitted at every action on ZeroParadox's request. Both were live push-blocking properties whose ONLY representation was a *_cleared.txt file, so retiring the signal path without them would have deleted coverage rather than migrated it. cr_cleared.txt is the sharper loss: without a claim_review type, deleting a check_frozen baseline entry becomes free.",
  "_check_frozen_retired_2026_08_23": "REMOVED from every action, mirroring the registry's actions:[] narrowing -- the same shape as `genesis`, which is admitted by nothing because the registry narrows it with a reason. The REASON lives in required.v2.json and not here: a list of names cannot explain itself, and a gate that vanished from a list nobody diffed is how a bar drops silently. check_frozen remains REGISTERED and its records remain valid; it simply no longer gates.",
  "_removing_an_entry_can_invalidate_a_downgrade_elsewhere": "⛔⛔ AN EXCLUSION HERE CAN SILENTLY RE-PRICE A DEFECT FILED SOMEWHERE ELSE, AND NOTHING IN THIS FILE SURFACES THAT. Every entry below states WHY it is excluded. None states WHAT WAS DOWNGRADED ON THE STRENGTH OF IT BEING THERE. Those are different facts and only the first is written down.\n\nMEASURED 2026-09-06, one day apart in August. The consumer filed `RLY31-6` on 2026-08-25 (a routing leg discharged by the very FAIL record it was produced to answer) and deliberately DOWNGRADED it, with the reason on the row: \"this is NOT the live bypass it was filed as. A `rely` FAIL row fails admission independently, so a discharged routing leg does not let a push through.\" That was TRUE when written. On 2026-08-26, `98c3480` removed `rely` from commit and push admission for two good and unrelated reasons (circularity, unsatisfiable-by-construction — see `_rely_not_admitted_at_commit_or_push`). The compensating control the downgrade rested on was withdrawn the next day. Nobody revisited the row. THE PRICE WAS NEVER RE-PAID, and it went live rather than known.\n\n⚠ The 2026-08-26 decision was CORRECT and is not in question. The defect is that a severity resting on a compensating control has no link to that control, so withdrawing the control cannot fail, warn, or even be noticed. Same shape as `GL-1`: a revamp kept every CONSUMER of the gate lock and dropped its only PRODUCER, and nothing broke loudly.\n\n⭐ SO, BEFORE REMOVING OR NARROWING ANY ENTRY BELOW: ask what was priced on it. A stated reason for an exclusion is necessary and is not sufficient — it explains the removal and says nothing about what the removal costs elsewhere. Whether that link should be MECHANISED rather than remembered is Tim's call and is not built.",
  "_prior_art_admitted_2026_09_17": "ADMITTED AT PUSH on Tim's ruling, 2026-09-17. ⛔ THE NOTE THIS REPLACES WAS TRUE AND EXPIRED: it said prior_art was removed 'only because the REGISTRY narrowed it to actions: []'. The registry re-widened it to actions: ['push'] on 2026-09-01, so from that date the stated reason no longer held and admission was the stale half of the pair -- for sixteen days, while the note still read as a current justification. ⚠ THE DEEPER REASON IS NOT DISCHARGED BY THIS: Tim recorded prior_art as claim-shaped ACKNOWLEDGED DEBT because no glob expresses what a run covers, and a wider glob does not answer that. What changed is that the step was doing the work and not counting: on 2026-09-15 one round examined 105 subjects, most outside its declared scope, including the prose homes of both recorded prior-art incidents. The scope widening (ZeroParadox a67ee7a6, corrected at b13e67cc) made the practice visible; this line makes it gate. ⚠ MEASURED COST AT THE MOMENT OF MIRRORING: scope 374 paths, 93 covered at current bytes, 276 NEVER EXAMINED and 5 STALE. The 276 are not a backlog to burn down -- each becomes gating the moment its file is touched, which is the intent and is a much larger surface than the version first proposed. The 5 stale block the next push until re-judged."
}
